x = input("Enter a word: ")

# The assert keyword will see if a statement is true. If true, nothing is executed in the Terminal. If false, then an error appears in the Terminal. You cannot enclose "assert" with
# a "print()" statement.

assert x == "Hello World"
